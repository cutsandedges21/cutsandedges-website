# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Raw 4K source video — too large for GitHub (>100MB files).
# Web-optimized versions are committed in public/videos/. See README to regenerate.
# Leading slash anchors to repo root only (Windows git is case-insensitive, so an
# unanchored "Videos/" would also wrongly match "public/videos/").
/Videos/

# Brainstorm / visual-companion session artifacts
.superpowers/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP artifacts
.playwright-mcp/
verify-*.jpeg
